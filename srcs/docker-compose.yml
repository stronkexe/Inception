version: "3"

services:
  nginx:
    image: mynginx
    ports:
      - "443:443"
    volumes:
      - /home/login/data:/src/usr/share/nginx/html
      - /etc/nginx/ssl:/etc/nginx/ssl
    restart: always
  mariadb:
    image: mdb
    restart: always
